@model IEnumerable<FoundItem>
@{
    Layout = "_SearchLayout";
}
@{
    ViewData["Title"] = "Search";

}
<style>

    @@media (min-width: 768px) {
        .col-md-3 {
            -webkit-box-flex: 0;
            -ms-flex: 0 0 25%;
            flex: 0 0 25%;
            max-width: 25%;
        }
    }
    .col-md-3 {
        position: relative;
        width: 100%;
        min-height: 1px;
        padding-right: 15px;
        padding-left: 15px;
        border-radius:5px;
    }
    .card-outline-primary {
        background-color: #263238;
        border-radius: 5px;
    }
    .card {
        position: relative;
        /*display: -webkit-box;*/
        display: -ms-flexbox;
        display: flex;
        -webkit-box-orient: vertical;
        -webkit-box-direction: normal;
        /*-ms-flex-direction: column;*/
        flex-direction: column;
        min-width: 0;
        word-wrap: break-word;
        /*background-color: #fff;*/
        /*background-clip: border-box;
        border: 1px solid rgba(0,0,0,.125);
        border-radius: .25rem;*/
        border-radius: 5px;
    }
    .card-header {
        padding: .75rem 1.25rem;
        margin-bottom: 0;
        border-bottom: 1px solid rgba(0,0,0,.125);
        background: #745af2;
        border-color: #745af2;
    }

        .card-header:first-child {
            border-radius: calc(.25rem - 1px) calc(.25rem - 1px) 0 0;
        }
    .card-body {
        -webkit-box-flex: 1;
        -ms-flex: 1 1 auto;
        flex: 1 1 auto;
        padding: 1.25rem;
        color:floralwhite;
    }

</style>

<div>
    <h3> Cant Find the Item you Found?</h3>
    <a asp-area="User" asp-controller="LostItem" asp-action="Create"><span> Register your Lost Item </span> </a>
</div>
@if (Model.Count() == 0)
{
    <div>No Found Item Matches </div>
}
else
{

    <div class="row">
        @foreach (var item in Model)
        {
        <div class="col-md-3">
            <div class="card card-outline-primary">
                <div class="card-header">
                    <h4 class="m-b-0 text-white">@item.NameOfFoundItem</h4>
                </div>
                <div class="card-body">
                    <h3 class="card-title text-white">@item.DateFound</h3>
                    <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
                    <a href="#" class="btn btn-inverse">Go somewhere</a>
                    <a asp-controller="FoundItemClaim" title="Claim" asp-action="SameUser" asp-route-id="@item.Id"><img style="height:40px; width: 40px" src="~/img/icon/lostclaimicon.png" /></a>

                </div>
            </div>
        </div>
                        @*<div class="progress-bar bg-info" role="progressbar" aria-valuenow="50" aria-valuemin="0" aria-valuemax="100" style="width:70%; height:3px;"> <span class="sr-only">50% Complete</span></div>
                    </div>
                    <a asp-controller="FoundItemClaim" title="Claim" asp-action="SameUser" asp-route-id="@item.Id"><img style="height:40px; width: 40px" src="~/img/icon/lostclaimicon.png" /></a>

                </div>*@
            
        }
    </div>
}